<div class="col-lg-8">
	<div class="card-columns">
		<%= render @lots %>
	</div>
	<%= will_paginate @lots, renderer: 'BootstrapPaginationHelper::LinkRenderer'  %>
</div>
<div class= "col-lg-3">
	<%= render 'filter' %>
</div>

