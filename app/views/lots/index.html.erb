<div class= "row">
	<div class="col-lg-9">
		<div class="card-columns">
			<%= render @lots %>
		</div>
		<%= will_paginate @lots, renderer: 'BootstrapPaginationHelper::LinkRenderer'  %>
	</div>
	<div class= "col-lg-3">
		<%= render 'filter' %>
	</div>
</div>
