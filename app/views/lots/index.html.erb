<div class="col-lg-9">
    <%= render 'filter' %>
    <br/>
    <div class="card-columns">
     <%= render @lots %>
    </div>
    <%= will_paginate @lots, renderer: 'BootstrapPaginationHelper::LinkRenderer'  %>
</div>

